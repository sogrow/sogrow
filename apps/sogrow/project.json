{
  "name": "sogrow",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/sogrow",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/sogrow",
        "outputPath": "dist/apps/sogrow"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/sogrow"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "sogrow:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "sogrow:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "sogrow:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "sogrow:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/sogrow/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/sogrow/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
